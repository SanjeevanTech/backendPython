# Production Dependencies (without heavy face recognition libraries)
# Face recognition is handled by ESP32 devices, not the server

# Core dependencies
pymongo==4.6.0
numpy==1.24.3
scikit-learn==1.3.2
requests==2.31.0
python-dateutil==2.8.2
