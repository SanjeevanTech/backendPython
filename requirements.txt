# Bus Passenger Tracking System - Python Dependencies

# Core dependencies
pymongo==4.6.0              # MongoDB driver
numpy==1.24.3               # Numerical operations for face embeddings
scikit-learn==1.3.2         # Cosine similarity calculations
requests==2.31.0            # HTTP requests for distance APIs
python-dateutil==2.8.2      # Date/time utilities
schedule==1.2.0             # Job scheduling for dynamic schedule manager

# Face recognition (ESP32-compatible MobileFaceNet via ONNX)
onnxruntime==1.16.1         # ONNX Runtime for model inference
opencv-python-headless==4.8.1.78 # Image processing without GUI
pillow==10.0.1              # Image manipulation

# NOTE: For Season Ticket registration via web interface, face-recognition is needed
# For ESP32 device entry/exit logs, face recognition is handled by ESP32 devices
